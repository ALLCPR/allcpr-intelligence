requests>=2.31.0
pandas>=2.0.0
openpyxl>=3.1.0  # read Enrollware .xlsx exports (Phase 4B); CSV needs no extra dep
python-dotenv>=1.0.0
tqdm>=4.66.0
pytest>=7.4.0
# Web dashboard (web_app.py), served on Render. httpx powers FastAPI's
# TestClient used by the web tests.
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.27.0
# OpenAI is optional; the pipeline runs without it. Uncomment to enable
# AI-generated executive summaries.
# openai>=1.0.0
